export default function Header({
  // className,
  // children
}: {
  // className?: string;
  // children?: React.ReactNode;
}) {
  return (
    <div className="header">
      <h1>HEADER</h1>
    </div>
  )
}